model:
  name: "cnn"
  num_classes: 10

data:
  name: "dummy"
  root: ""
  image_size: 64

trainer:
  batch_size: 32
  lr: 1e-3
  max_epochs: 10
  num_workers: 4
  device: "cuda"
  precision: "32"
  log_dir: "runs"
  seed: 42
  cudnn_deterministic: false
